[package]
name = "endfield-calc"
version = "1.0.0"
description = "엔드필드 공장 계산기"
authors = ["mincho8317"]
license = "MIT"
repository = "https://github.com/mincho8317/endfield.calc"
edition = "2021"
rust-version = "1.70"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
tauri = { version = "1", features = [
  "system-tray",
  "updater",
  "window-all",
  "global-shortcut-all",
  "process-exit",
  "shell-open",
  "protocol-asset",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
